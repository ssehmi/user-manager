import { styled } from '@material-ui/core/styles';
import Card from '@material-ui/core/Card';

const UserCardItem = styled(Card)({
    height: '100%',
    display: 'flex',
    flexDirection: 'column',
});

export default UserCardItem;
